<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.konsulyuk">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Pasien.InfoPsikologActivity"></activity>
        <activity android:name=".Pasien.HasilPenilaianActivity" />
        <activity android:name=".Pasien.PenilaianDiriActivity" />
        <activity android:name=".CallActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".Pasien.SelectPsikologActivity" />
        <activity
            android:name=".Psikolog.HomePsikologActivity"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".Pasien.PaketActivity"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".LoginActivity"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".Pasien.HomeActivity"
            android:theme="@style/MyActionBar" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/MyActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>